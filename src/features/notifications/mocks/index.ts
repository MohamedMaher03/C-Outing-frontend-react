/**
 * Notifications Feature — Mock barrel
 *
 * Re-exports the mock API and seed data from notificationsMock.ts.
 * All seed data lives in notificationsMock.ts to keep the dependency
 * graph acyclic.
 */

export { notificationsMock, MOCK_NOTIFICATIONS } from "./notificationsMock";
