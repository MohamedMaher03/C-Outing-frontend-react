/**
 * Auth Feature — Mocks Barrel
 *
 * Re-exports:
 *   • authMock  — drop-in replacement for authApi (use during development)
 *   • Form field constants used by login / sign-up forms
 */

export { authMock } from "./authMock";
export { LOGIN_FORM_FIELDS, SIGN_UP_FORM_FIELDS } from "./formFields";
